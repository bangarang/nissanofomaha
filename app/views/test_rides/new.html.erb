<% provide(:title, 'Schedule a Test Drive') %>
<div class="container">
	<h2 class="form_title">Schedule a Joy Ride</h2>

	<%= render 'form' %>

	<%= link_to 'Back', test_rides_path if signed_in? %>
</div>
